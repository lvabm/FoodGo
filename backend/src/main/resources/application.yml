server:
  # Đảm bảo cổng Backend đồng nhất (cho Frontend Proxy)
  port: 8080

spring:
  # PHẦN 1: THIẾT LẬP KẾT NỐI DATASOURCE
  datasource:
    # For development with Docker (H2 in-memory database)
    url: jdbc:h2:mem:foodgo_db
    username: sa
    password: 
    driver-class-name: org.h2.Driver

  # PHẦN 2: CẤU HÌNH JPA/HIBERNATE
  jpa:
    defer-datasource-initialization: true
    hibernate:
      # update: Phù hợp cho Dev, tự động cập nhật schema. Cẩn thận khi dùng
      # create-drop: Tự động tạo và xóa schema sau khi ứng dụng tắt (tùy chọn)
      # Dùng create xong đổi sang update là ok (Chạy 2 lần)
      # ddl-auto: update
      ddl-auto: create-drop
      database-platform: org.hibernate.dialect.H2Dialect

    # Hiển thị câu lệnh SQL trong console (Giúp Debug và tối ưu)
    show-sql: true
    properties:
      hibernate:
        format_sql: true # Định dạng SQL dễ đọc hơn

  # PHẦN 3: CẤU HÌNH HOT-RELOAD & DEVTOOLS (Tối ưu DevEx)
  devtools:
    # Tự động khởi động lại (restart) ứng dụng khi có thay đổi code
    restart:
      enabled: true
    # Kích hoạt LiveReload cho trình duyệt (Nếu bạn có cài LiveReload extension)
    livereload:
      enabled: true

  # PHẦN 4: TẮT DOCKER COMPOSE AUTO-CONFIGURATION
  docker:
    compose:
      enabled: false

jwt:
  # 🔑 BẮT BUỘC: Chuỗi này phải là Base64-encoded và có độ dài ít nhất 256 bits (>= 32 ký tự sau khi decode)
  secret: MTAwUGVyY2VudFNlY3JldEtleUZvckZvb2RHb0JhY2tlbmRNYXlCRTEyMzQ1Njc4OQ==
  # Thời gian hết hạn (Phút)
  exp-minutes: 60

google:
  maps:
    # Google Maps API Key - Lấy từ: https://console.cloud.google.com/google/maps-apis
    # Để trống nếu chưa có API key (tính năng sẽ bị vô hiệu hóa)
    api-key: ${GOOGLE_MAPS_API_KEY:}

app:
  moderation:
    # Bật/tắt tự động duyệt review
    auto-approve-enabled: true
    # Số lượng báo cáo để tự động ẩn review
    auto-hide-threshold: 3
    # Bật/tắt kiểm tra spam
    spam-check-enabled: true
    # Bật/tắt kiểm tra từ ngữ không phù hợp
    profanity-check-enabled: true
    # Base URL của Google Maps API
    base-url: https://maps.googleapis.com/maps/api
    # Bật/tắt tính năng Google Maps
    enabled: true
    # Timeout cho API calls (milliseconds)
    timeout: 5000